# Base URL for the LeafMind FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Human-readable app name, surfaced in the UI shell.
VITE_APP_NAME=LeafMind

# Current app environment label (development | staging | production).
VITE_APP_ENV=development
