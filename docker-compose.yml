services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: leafmind
      POSTGRES_PASSWORD: leafmind
      POSTGRES_DB: leafmind
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leafmind"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://leafmind:leafmind@postgres:5432/leafmind
      # Wildcard ngrok subdomains are matched via allow_origin_regex in
      # app/main.py instead — CORSMiddleware's allow_origins only supports
      # exact string matches, so a "*.ngrok-free.app" entry here would
      # silently never match anything.
      CORS_ORIGINS: http://leafmind.local,http://localhost:5173,http://100.66.5.122
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./datasets:/datasets:ro
      - backend_uploads:/app/uploads
      - backend_documents:/app/documents
      - backend_chroma:/app/chroma_data
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  frontend:
    build: ./frontend
    environment:
      VITE_API_BASE_URL: /api/v1
      VITE_APP_NAME: LeafMind
      VITE_APP_ENV: development
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend

  tunnel:
    image: ngrok/ngrok:latest
    command: http nginx:80
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040"
    depends_on:
      - nginx

  deploy-webhook:
    build: ./deploy-webhook
    environment:
      WEBHOOK_SECRET: ${WEBHOOK_SECRET}
      REPO_DIR: ${REPO_DIR:-/repo}
      DEPLOY_BRANCH: main
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${REPO_DIR:-.}:${REPO_DIR:-/repo}
    ports:
      - "9000:9000"

volumes:
  postgres_data:
  backend_uploads:
  backend_documents:
  backend_chroma:
